export const IS_PUBLIC_KEY_PERMISSION = 'isPublic';

export enum ACTIVE_STATUS {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE'
}

export enum USER_TYPE {
    ADMIN = 'ADMIN',
    USER = 'USER'
}

export enum TRANSACTION_TYPE {
    IN = 'IN',
    OUT = 'OUT'
}